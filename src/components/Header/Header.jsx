import ButtonAdd from "../ButtonAdd/ButtonAdd";
import "./Header.scss";

const Header = () => {
  return (
    <header className="header">
      <ButtonAdd />
    </header>
  );
};

export default Header;
